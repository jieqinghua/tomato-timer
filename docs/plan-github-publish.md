# GitHub 发布计划

## 目标与范围

- 将当前 `tomato-timer` 项目完整发布到 GitHub。
- 发布范围包含当前 `codex/v2.0` 分支上已提交的历史，以及工作区内与 v2.0 版本相关的源码、测试、资源、文档和演示文件变更。
- 不在认证和远程仓库信息明确前暂存、提交或推送文件。

## 执行步骤及状态

| 步骤 | 状态 | 说明 |
| --- | --- | --- |
| 检查 Git 状态、当前分支和变更范围 | 已完成 | 当前分支为 `codex/v2.0`；工作区包含 v2.0 源码、测试、资源、文档和演示文件变更。 |
| 检查 GitHub CLI 与认证 | 已完成 | 已验证账号 `jieqinghua` 登录有效。 |
| 检查 GitHub 远程仓库 | 已完成 | 已创建私有仓库 `jieqinghua/tomato-timer` 并配置为 `origin`。 |
| 检查超大文件和明显敏感文件 | 已完成 | 未发现超过 50 MB 的文件，未发现常见证书、环境变量或签名配置文件。 |
| 确认远程仓库与可见性 | 已完成 | 用户已确认创建新的私有 GitHub 仓库。 |
| 暂存并提交当前项目变更 | 已完成 | 已创建提交 `c882778`（`feat: complete tomato timer v2.0`）。 |
| 运行相关测试 | 已完成 | macOS 目标的 `TimerViewModelTests` 测试套件全部通过。 |
| 推送 `codex/v2.0` 并设置上游 | 已完成 | 已推送 `master`、`codex/v2.0` 和 `v1.0.0` 标签；当前分支已跟踪远程。 |
| 创建草稿 PR | 已完成 | 已创建 [PR #1](https://github.com/jieqinghua/tomato-timer/pull/1)，目标分支为 `master`。 |

## 关键决策

- 保留当前 `codex/v2.0` 分支，不额外创建新分支。
- 新仓库名称采用项目目录名 `tomato-timer`，可见性设为私有。
- 发布前按完整项目工作区核对变更，但不会在范围不清楚时静默暂存文件。
- 默认使用草稿 PR，除非用户明确要求直接合并或发布为正式 PR。
- 保留 `master` 作为 v1.0 基线，并通过草稿 PR 展示 v2.0 的完整变更。

## 风险或待确认事项

- 仓库为私有可见性；只有获授权的 GitHub 用户可以访问代码和 PR。
- 工作区中的演示视频发生文件名替换：删除 `demo/fanqieshizhong.mp4`，新增 `demo/番茄时钟v2.0演示.mp4`；发布时将按当前工作区状态处理。

## 最后更新时间

- 2026-07-03 22:31（Asia/Shanghai）
